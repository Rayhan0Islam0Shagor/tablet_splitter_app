/**
 * Minimum size for a new tablet (initial creation)
 */
export const MIN_TABLET_WIDTH = 40; // dpi
export const MIN_TABLET_HEIGHT = 20; // dpi

/**
 * Minimum size for a tablet part (after splitting)
 */
export const MIN_PART_WIDTH = 20; // dpi
export const MIN_PART_HEIGHT = 10; // dpi

/**
 * Default corner radius for tablets
 */
export const DEFAULT_BORDER_RADIUS = 12;

/**
 * Threshold distance to distinguish between tap and drag
 */
export const TAP_THRESHOLD = 10; // pixels

/**
 * Gap between split tablet parts (visual separation)
 */
export const SPLIT_GAP = 4; // pixels